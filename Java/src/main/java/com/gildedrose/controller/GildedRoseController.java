package com.gildedrose.controller;

/*import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.web.bind.annotation.*;

@RestController
@RequestMapping("/gildedrose")
public class GildedRoseController {

    private final GildedRoseService gildedRoseService;

    @Autowired
    public GildedRoseController(GildedRoseService gildedRoseService) {
        this.gildedRoseService = gildedRoseService;
    }

    // POST endpoint to update quality of items
    @PostMapping("/updateQuality")
    public Item[] updateQuality(@RequestBody Item[] items) {
        gildedRoseService.updateGildRoseQuality(items);
        return items;  // returning updated items in response
    }


}*/
